# Client-side environment variables (safe to expose in browser)
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_APP_URL=http://localhost:5173

# ── REMOVED (were incorrectly set as VITE_ client vars) ──────────────────────
# VITE_SUPABASE_SERVICE_ROLE_KEY  ← NEVER expose this client-side
# VITE_RESEND_API_KEY             ← NEVER expose this client-side
# VITE_FROM_EMAIL                 ← moved to edge function env
#
# Set these as Supabase Edge Function secrets instead:
#   supabase secrets set RESEND_API_KEY=re_...
#   supabase secrets set FROM_EMAIL=noreply@send.yourdomain.com
#   supabase secrets set APP_URL=https://orgchart.aveyo.com
# (SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are provided automatically)
